language: ruby
cache:
  bundler: true
  yarn: true
  directories:
     - /home/travis/.rvm/
rvm:
 - 2.6.3
before_install:
 - nvm install 15.6
install:
 - bundle install
 - yarn install
script:
 - bundle exec rails db:migrate
 - bundle exec rails test
